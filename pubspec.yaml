name: tag_field
description: A customizable Flutter widget for adding and managing tags.
version: 1.0.7
homepage: https://github.com/afaqxdev/tag_field # optional but recommended
repository: https://github.com/afaqxdev/tag_field
issue_tracker: https://github.com/afaqxdev/tag_field/issues
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  flutter_lints:

dev_dependencies:
  flutter_test:
    sdk: flutter

assets:
  - asset/
